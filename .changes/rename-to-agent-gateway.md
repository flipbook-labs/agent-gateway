---
bump: minor
category: Changes
---

Rename the package from `PluginAgents` to `AgentGateway` to better reflect its role: it is the gateway that exposes plugin actions to agents, not a package that defines what agents are.